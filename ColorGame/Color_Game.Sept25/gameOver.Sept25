void gameOver() { //60fps - see draw function in Color_Game
  background(255,0,0);
  textAlign(CENTER,CENTER);
  textSize(50);
  fill(255);
  text("Game over, try again", width/2, height/4);
}

void gameOverMouseReleased() {
  
}
